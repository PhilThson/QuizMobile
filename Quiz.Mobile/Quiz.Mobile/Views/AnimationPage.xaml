<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Quiz.Mobile.Views.AnimationPage"
    xmlns:viewmodels="clr-namespace:Quiz.Mobile.ViewModels"         
    x:DataType="viewmodels:ImageCacheViewModel"
    Title="Animations"
    xmlns:fonts="clr-namespace:Quiz.Mobile.Helpers.Icons"
    BackgroundColor="{AppThemeBinding 
                        Dark={StaticResource WindowBackgroundColorDark}, 
                        Light={StaticResource WindowBackgroundColor}}">

    <ContentPage.BindingContext>
        <viewmodels:ImageCacheViewModel />
    </ContentPage.BindingContext>

    <StackLayout>
        <Button Text="Animate"
                Command="{Binding RefreshLongCommand}"
                IsEnabled="{Binding IsNotBusy}"
                />
                <!--Clicked="Button_Clicked"-->

        <!--Pokazuje się kiedy jest podniesiona flaga IsBusy-->
        <Label
               FontFamily="FAB"
               Text="{x:Static fonts:FontAwesomeIcons.Spinner}"
               x:Name="LabelLoad"
               IsVisible="{Binding IsBusy}"
               FontSize="80"
               Style="{StaticResource LabelLarge}"
               HorizontalOptions="Center"/>

        <Image x:Name="fileImage" Source="{Binding Image}" Aspect="AspectFit"
               HeightRequest="60" WidthRequest="70"/>
    </StackLayout>
    
</ContentPage>

